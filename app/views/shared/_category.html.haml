.items__content
  -# ここでrender
  .items__content__block
  %h3.items__content__block__name
    =category.name 新着アイテム
  .items__content__block__description
    =link_to category_path(category) do
      もっと見る
    =link_to category_path(category) do
      %i.fas.fa-angle-right
  -# ここは_item.html.hamlをrenderします
  =link_to item_path(item), class:"item-link" do
    .item
      =image_tag itemimage.url, alt:"item.text", class:""
      .item__price
        -# ¥と,をつけるメソッドが必要
        =item.price 
      .item__imag 
        =item.name