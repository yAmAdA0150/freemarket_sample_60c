=link_to item_path(item), class:"item-link" do
  .item
    =image_tag itemimage.url, alt:"item.text", class:""
    .item__price
      -# ¥と,をつけるメソッドが必要
      =item.price 
    .item__imag 
      =item.name