.camera
  -if user_signed_in?
    = link_to new_user_item_path(current_user), class: 'link' do
      .camera__main
        .camera__main__text
          出品
        .camera__main__mark
          = fa_icon 'camera', class: 'icon'
  -else
    = link_to user_session_path, class: 'link' do
      .camera__main
        .camera__main__text
          出品
        .camera__main__mark
          = fa_icon 'camera', class: 'icon'